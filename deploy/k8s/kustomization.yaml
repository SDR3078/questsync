apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# The sealed secret (questsync-habitica) and the Ingress are intentionally NOT
# listed here — apply the secret out-of-band and only add ingress.yaml once DAV
# auth is enabled (see deploy/README.md).
resources:
  - deployment.yaml
  - service.yaml
